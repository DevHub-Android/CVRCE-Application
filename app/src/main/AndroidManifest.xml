<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.devhub.use.cvrceapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Globals.Globals"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/CustomAppTheme" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/CustomAppTheme" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ComplaintsActivity"
            android:label="Complaint Details"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.android.devhub.use.cvrceapplication.HomeActivity" />
        </activity>
        <activity android:name=".AddComplaint" />
        <activity android:name=".MentorActivity">

        </activity>
        <activity android:name=".ShowStudentsActivity" />
        <activity android:name=".MentorLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>